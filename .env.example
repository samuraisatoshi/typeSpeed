# TypeSpeed Application Configuration

# Server Configuration
PORT=3000

# Security Configuration

# Trusted CORS Origins (comma-separated)
# Add your frontend domains here
TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Rate Limiting
# Maximum number of requests per minute per IP
MAX_REQUESTS_PER_MINUTE=60

# Note: Additional allowed scan paths can be configured in code
# See SecurityConfig in src/domain/shared/SecurityConfig.ts
